name: sr_rom
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - python=3.11.9
  - numpy=1.26.4
  - pip:
    - torch=2.1.0
    - joblib
    - scikit-learn
    - skorch
    - pyoperon
    - pytest
    - pytest-cov